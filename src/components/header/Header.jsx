import React from "react";
import styles from "./header.module.css";


const Header = () => {
  return (<header className={styles.head}>
    <span className={styles.title}>Create Business Card</span>
  </header>)
}
export default Header;